import pandas as pd
s = pd.Series([1, 2, 3, 4, 5])
print(s)

# datatype integer as we have integer values alone
# if i add any other value other than integer then the datatype will be changed to object